import React from "react";

const MainContent: React.FunctionComponent = () => {
  return (
    <p>there will be switch case logic here to swap out content based on context</p>
  );
};

export default MainContent;